# Results were produced with Python 3.13.9 on Windows 10 Pro.
# The greedy quadratization and the Qiskit transpiler are both sensitive to
# library version, and COBYLA is provided by SciPy, so the pinned packages
# below should be matched exactly in order to reproduce the reported ancilla
# counts, transpiled circuits and optimizer evaluation counts.

numpy==2.3.5
qiskit==2.5.2
scipy==1.16.3
qiskit-ibm-runtime
